<Page x:Name="RootPage"
    x:Class="SystemOptimizer.Views.Pages.NetworkPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SystemOptimizer.ViewModels">

    <Grid Padding="28" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Rede" Style="{StaticResource TitleTextBlockStyle}" />

        <CommandBar Grid.Row="1" DefaultLabelPosition="Right" Background="Transparent">
            <AppBarButton Label="Aplicar selecionados"
                          Icon="Accept"
                          Command="{Binding ApplySelectedCommand}"
                          CommandParameter="Network" />
            <AppBarButton Label="Restaurar selecionados"
                          Icon="Undo"
                          Command="{Binding RevertSelectedCommand}"
                          CommandParameter="Network" />
        </CommandBar>

        <ListView Grid.Row="2" ItemsSource="{Binding NetworkTweaks}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:TweakViewModel">
                    <Grid Padding="16" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <CheckBox IsChecked="{x:Bind IsSelected, Mode=TwoWay}" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock Text="{x:Bind Title}" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="Wrap" />
                            <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" Opacity="0.72" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="{x:Bind StatusIcon, Mode=OneWay}" Foreground="{x:Bind StatusColor, Mode=OneWay}" />
                                <TextBlock Text="{x:Bind StatusText, Mode=OneWay}" Foreground="{x:Bind StatusColor, Mode=OneWay}" FontWeight="SemiBold" />
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button Content="Aplicar" Command="{Binding ElementName=RootPage, Path=DataContext.ApplySingleCommand}" CommandParameter="{x:Bind Id}" />
                            <Button Content="Restaurar" Command="{Binding ElementName=RootPage, Path=DataContext.RevertSingleCommand}" CommandParameter="{x:Bind Id}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
