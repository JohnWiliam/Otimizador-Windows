<Page x:Class="SystemOptimizer.Views.Pages.CleanupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:SystemOptimizer.ViewModels"
      xmlns:p="clr-namespace:SystemOptimizer.Properties"
      xmlns:helpers="clr-namespace:SystemOptimizer.Helpers"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      x:Name="RootPage"
      Title="CleanupPage"
      FontFamily="Segoe UI">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="{x:Static p:Resources.Cleanup_Title}" 
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                   FontSize="26" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,16" />

        <ui:CardExpander Grid.Row="1" Margin="0,0,0,16" 
                         IsExpanded="{Binding IsOptionsExpanded, ElementName=RootPage, Mode=TwoWay}">
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" /> </Grid.ColumnDefinitions>
                    
                    <ui:SymbolIcon Symbol="Broom24" FontSize="24" Margin="0,0,12,0" />
                    
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{x:Static p:Resources.Cleanup_CacheTitle}" 
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                   FontSize="15" 
                                   FontWeight="Medium" />
                        <TextBlock Text="{x:Static p:Resources.Cleanup_CacheDesc}"
                                   FontSize="13" 
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                                   Margin="0,4,0,0" 
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <Grid Grid.Column="2" VerticalAlignment="Center" Margin="16,0,8,0">
                        
                        <ui:Button Command="{Binding ExecuteSelectedCleanupCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                   Appearance="Primary"
                                   Height="36"
                                   Padding="16,0"
                                   Visibility="{Binding IsBusyLocal, ElementName=RootPage, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Play24" Margin="0,0,8,0"/>
                                <TextBlock Text="{x:Static p:Resources.Btn_RunCleanup}"/>
                            </StackPanel>
                        </ui:Button>

                        <ui:Button Appearance="Primary"
                                   Height="36"
                                   Padding="16,0"
                                   Cursor="Wait"
                                   Visibility="{Binding IsBusyLocal, ElementName=RootPage, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal">
                                <ui:ProgressRing IsIndeterminate="True" Width="16" Height="16" Margin="0,0,8,0" Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"/>
                                <TextBlock Text="{x:Static p:Resources.Status_Applying}" Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </ui:Button>
                    </Grid>
                </Grid>
            </ui:CardExpander.Header>

            <StackPanel Margin="0,8,0,0">
                <TextBlock Text="{x:Static p:Resources.Cleanup_SelectOptions}" 
                           FontWeight="SemiBold" Margin="0,0,0,12" 
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                
                <UniformGrid Columns="2" Rows="4">
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionTemp}" 
                              IsChecked="{Binding CleanTemp, ElementName=RootPage}" Margin="0,0,8,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionSystemTemp}" 
                              IsChecked="{Binding CleanSystemTemp, ElementName=RootPage}" Margin="0,0,8,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionPrefetch}" 
                              IsChecked="{Binding CleanPrefetch, ElementName=RootPage}" Margin="0,0,8,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionWindowsUpdate}" 
                              IsChecked="{Binding CleanWindowsUpdate, ElementName=RootPage}" Margin="0,0,8,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionBrowser}" 
                              IsChecked="{Binding CleanBrowser, ElementName=RootPage}" Margin="0,0,8,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionDNS}" 
                              IsChecked="{Binding CleanDns, ElementName=RootPage}" Margin="0,0,8,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionRecycleBin}" 
                              IsChecked="{Binding CleanRecycleBin, ElementName=RootPage}" Margin="0,0,8,12"
                              Foreground="{DynamicResource PaletteRedBrush}"/>
                </UniformGrid>
            </StackPanel>
        </ui:CardExpander>

        <ui:Card Grid.Row="2" 
                 Padding="0" 
                 VerticalAlignment="Stretch" 
                 VerticalContentAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" 
                        Padding="16,10" 
                        Background="{DynamicResource ControlFillColorSecondaryBrush}"
                        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="0,0,0,1"
                        CornerRadius="4,4,0,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <ui:SymbolIcon Symbol="CodeText20" Margin="0,0,10,0" FontSize="16"/>
                        <TextBlock Text="{x:Static p:Resources.Cleanup_LogTitle}" 
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                   FontWeight="Medium" 
                                   FontSize="13" />
                    </StackPanel>
                </Border>

                <RichTextBox Grid.Row="1" x:Name="LogOutput" 
                             IsReadOnly="True" 
                             Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                             Background="Transparent" 
                             BorderThickness="0" 
                             Padding="16"
                             VerticalScrollBarVisibility="Auto"
                             VerticalContentAlignment="Top"
                             FontFamily="Segoe UI" 
                             FontSize="12">
                    <FlowDocument PagePadding="0" TextAlignment="Left" FontFamily="Segoe UI">
                    </FlowDocument>
                </RichTextBox>
            </Grid>
        </ui:Card>
    </Grid>
</Page>
