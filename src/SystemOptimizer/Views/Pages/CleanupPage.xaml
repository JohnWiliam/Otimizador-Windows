<Page x:Class="SystemOptimizer.Views.Pages.CleanupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:p="clr-namespace:SystemOptimizer.Properties"
      mc:Ignorable="d" 
      d:DesignHeight="760" d:DesignWidth="1040"
      x:Name="RootPage"
      Title="CleanupPage"
      FontFamily="Segoe UI">

    <Page.Resources>
        <QuadraticEase x:Key="CleanupEaseOut" EasingMode="EaseOut" />

        <Style x:Key="HeroActionButtonStyle" TargetType="ui:Button">
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="18,0" />
            <Setter Property="Margin" Value="0,0,10,10" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="PreviewMouseLeftButtonDown">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="0.985" Duration="0:0:0.10" EasingFunction="{StaticResource CleanupEaseOut}" />
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0.985" Duration="0:0:0.10" EasingFunction="{StaticResource CleanupEaseOut}" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="PreviewMouseLeftButtonUp">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.18" EasingFunction="{StaticResource CleanupEaseOut}" />
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.18" EasingFunction="{StaticResource CleanupEaseOut}" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.16" EasingFunction="{StaticResource CleanupEaseOut}" />
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.16" EasingFunction="{StaticResource CleanupEaseOut}" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="OptionTileStyle" TargetType="Border">
            <Setter Property="Margin" Value="0,0,12,12" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="MinWidth" Value="235" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="0" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="-3" Duration="0:0:0.20" EasingFunction="{StaticResource CleanupEaseOut}" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.22" EasingFunction="{StaticResource CleanupEaseOut}" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border x:Name="HeroPanel"
                Grid.Row="0"
                Margin="0,0,0,18"
                Padding="22"
                CornerRadius="18"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border Width="58" Height="58" CornerRadius="18" Background="#220078D4" VerticalAlignment="Top">
                    <ui:SymbolIcon Symbol="Broom24" FontSize="30" Foreground="#4DA3FF" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>

                <StackPanel Grid.Column="1" Margin="18,0,28,0">
                    <TextBlock Text="{x:Static p:Resources.Cleanup_Title}"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               FontSize="30"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{x:Static p:Resources.Cleanup_CacheDesc}"
                               Margin="0,8,0,14"
                               FontSize="14"
                               LineHeight="21"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap" />

                    <WrapPanel>
                        <ui:Button Command="{Binding AnalyzeCommand}"
                                   IsEnabled="{Binding CanAnalyze}"
                                   Appearance="Primary"
                                   Style="{StaticResource HeroActionButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Search24" Margin="0,0,8,0" />
                                <TextBlock Text="{x:Static p:Resources.Cleanup_ActionAnalyze}" />
                            </StackPanel>
                        </ui:Button>

                        <ui:Button Command="{Binding CleanupSelectedCommand}"
                                   IsEnabled="{Binding CanCleanup}"
                                   Appearance="Primary"
                                   Style="{StaticResource HeroActionButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Delete24" Margin="0,0,8,0" />
                                <TextBlock Text="{x:Static p:Resources.Cleanup_ActionClean}" />
                            </StackPanel>
                        </ui:Button>

                        <ui:Button Command="{Binding CancelCommand}"
                                   Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                                   Appearance="Secondary"
                                   Style="{StaticResource HeroActionButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Dismiss24" Margin="0,0,8,0" />
                                <TextBlock Text="{x:Static p:Resources.Cleanup_ActionCancel}" />
                            </StackPanel>
                        </ui:Button>
                    </WrapPanel>
                </StackPanel>

                <ui:Card Grid.Column="2" MinWidth="210" Padding="16" VerticalAlignment="Stretch">
                    <StackPanel>
                        <TextBlock Text="Status da sessão" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="{Binding CleanupProgressCategory}" Margin="0,7,0,10" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" />
                        <ProgressBar Height="7" Minimum="0" Maximum="100" Value="{Binding CleanupProgressPercentage}" />
                        <TextBlock Text="{Binding CleanupProcessedItemsLabel}" Margin="0,10,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </ui:Card>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.08*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="0.92*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ui:CardExpander x:Name="OptionsCard"
                             Grid.Row="0"
                             Grid.Column="0"
                             Padding="0"
                             Margin="0,0,0,16"
                             IsExpanded="{Binding IsOptionsExpanded, Mode=TwoWay}">
                <ui:CardExpander.Header>
                    <Grid Margin="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="38" Height="38" CornerRadius="12" Background="#1A4DB6AC" Margin="0,0,12,0">
                            <ui:SymbolIcon Symbol="CheckmarkCircle24" Foreground="#4DB6AC" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{x:Static p:Resources.Cleanup_CacheTitle}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                            <TextBlock Text="{x:Static p:Resources.Cleanup_SelectOptions}" Margin="0,3,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </Grid>
                </ui:CardExpander.Header>

                <WrapPanel Margin="4,16,0,4">
                    <Border Style="{StaticResource OptionTileStyle}">
                        <CheckBox Content="{x:Static p:Resources.Cleanup_OptionTemp}" IsChecked="{Binding CleanTemp}" />
                    </Border>
                    <Border Style="{StaticResource OptionTileStyle}">
                        <CheckBox Content="{x:Static p:Resources.Cleanup_OptionSystemTemp}" IsChecked="{Binding CleanSystemTemp}" />
                    </Border>
                    <Border Style="{StaticResource OptionTileStyle}">
                        <CheckBox Content="{x:Static p:Resources.Cleanup_OptionPrefetch}" IsChecked="{Binding CleanPrefetch}" />
                    </Border>
                    <Border Style="{StaticResource OptionTileStyle}">
                        <CheckBox Content="{x:Static p:Resources.Cleanup_OptionWindowsUpdate}" IsChecked="{Binding CleanWindowsUpdate}" />
                    </Border>
                    <Border Style="{StaticResource OptionTileStyle}">
                        <CheckBox Content="{x:Static p:Resources.Cleanup_OptionBrowser}" IsChecked="{Binding CleanBrowser}" />
                    </Border>
                    <Border Style="{StaticResource OptionTileStyle}">
                        <CheckBox Content="{x:Static p:Resources.Cleanup_OptionDNS}" IsChecked="{Binding CleanDns}" />
                    </Border>
                    <Border Style="{StaticResource OptionTileStyle}">
                        <CheckBox IsChecked="{Binding CleanRecycleBin}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Delete20" Margin="0,0,6,0" Foreground="#D46A6A" />
                                <TextBlock Text="{x:Static p:Resources.Cleanup_OptionRecycleBin}" />
                            </StackPanel>
                        </CheckBox>
                    </Border>
                </WrapPanel>
            </ui:CardExpander>

            <ui:Card x:Name="SummaryCard"
                     Grid.Row="1"
                     Grid.Column="0"
                     Padding="0"
                     Visibility="{Binding ShouldShowSummaryCard, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Padding="18,14" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" CornerRadius="8,8,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:SymbolIcon Symbol="DataHistogram20" Margin="0,0,10,0" FontSize="18" />
                                <StackPanel>
                                    <TextBlock Text="{x:Static p:Resources.Cleanup_ScanSummaryTitle}" FontSize="15" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding ScanResultCountLabel}" Margin="0,2,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                </StackPanel>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding TotalPotentialSizeLabel}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
                        </Grid>
                    </Border>

                    <Grid Grid.Row="1" Margin="18">
                        <TextBlock Text="{x:Static p:Resources.Cleanup_ScanSummaryEmpty}"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Top"
                                   Visibility="{Binding HasScanResults, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />

                        <ScrollViewer VerticalScrollBarVisibility="Auto" Visibility="{Binding HasScanResults, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ItemsControl ItemsSource="{Binding ScanResults}">
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Opacity" Value="0" />
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <TranslateTransform Y="12" />
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.28" EasingFunction="{StaticResource CleanupEaseOut}" />
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.32" EasingFunction="{StaticResource CleanupEaseOut}" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,10" Padding="14" CornerRadius="12" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" Margin="0,0,12,0" VerticalAlignment="Center" />
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                                    <TextBlock Text="{Binding ItemsLabel}" Margin="0,3,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                                </StackPanel>
                                                <TextBlock Grid.Column="2" Text="{Binding SizeLabel}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </ui:Card>

            <ui:Card x:Name="LogsCard"
                     Grid.Row="0"
                     Grid.RowSpan="2"
                     Grid.Column="2"
                     Padding="0"
                     RenderTransformOrigin="0.5,0.5"
                     VerticalAlignment="Stretch"
                     VerticalContentAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Padding="18,14" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" CornerRadius="8,8,0,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="CodeText20" Margin="0,0,10,0" FontSize="18" />
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Resources.Cleanup_LogTitle}" FontWeight="SemiBold" FontSize="15" />
                                <TextBlock Text="Auditoria em tempo real, com eventos seguros e canceláveis" Margin="0,2,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <RichTextBox Grid.Row="1" x:Name="LogOutput"
                                 IsReadOnly="True"
                                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="18"
                                 VerticalScrollBarVisibility="Auto"
                                 VerticalContentAlignment="Top"
                                 FontFamily="Segoe UI"
                                 FontSize="12">
                        <FlowDocument PagePadding="0" TextAlignment="Left" FontFamily="Segoe UI" />
                    </RichTextBox>

                    <TextBlock Grid.Row="1"
                               IsHitTestVisible="False"
                               Text="{x:Static p:Resources.Cleanup_LogEmpty}"
                               Margin="18"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               VerticalAlignment="Top"
                               Visibility="{Binding HasLogs, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                </Grid>
            </ui:Card>
        </Grid>
    </Grid>
</Page>
