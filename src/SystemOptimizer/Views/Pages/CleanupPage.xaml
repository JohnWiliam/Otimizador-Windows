<Page x:Class="SystemOptimizer.Views.Pages.CleanupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:p="clr-namespace:SystemOptimizer.Properties"
      mc:Ignorable="d"
      d:DesignHeight="760" d:DesignWidth="980"
      x:Name="RootPage"
      Title="CleanupPage"
      FontFamily="Segoe UI">

    <Page.Resources>
        <CubicEase x:Key="FluidEase" EasingMode="EaseOut"/>

        <Style x:Key="ActionButtonStyle" TargetType="ui:Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.025" Duration="0:0:0.18" EasingFunction="{StaticResource FluidEase}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.025" Duration="0:0:0.18" EasingFunction="{StaticResource FluidEase}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.20" EasingFunction="{StaticResource FluidEase}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.20" EasingFunction="{StaticResource FluidEase}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="PreviewMouseLeftButtonDown">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="0.965" Duration="0:0:0.10"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0.965" Duration="0:0:0.10"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="PreviewMouseLeftButtonUp">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.025" Duration="0:0:0.14" EasingFunction="{StaticResource FluidEase}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.025" Duration="0:0:0.14" EasingFunction="{StaticResource FluidEase}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MetricCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,12,12"/>
            <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="CleanupOptionStyle" TargetType="CheckBox">
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border x:Name="HeroPanel"
                    Grid.Row="0"
                    CornerRadius="28"
                    Padding="28"
                    Margin="0,0,0,18"
                    RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <TranslateTransform Y="0"/>
                </Border.RenderTransform>
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#2536A5" Offset="0"/>
                        <GradientStop Color="#0E8C86" Offset="0.58"/>
                        <GradientStop Color="#10233E" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Centro de limpeza inteligente"
                                   Foreground="#CCFFFFFF"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <TextBlock Text="{x:Static p:Resources.Cleanup_Title}"
                                   Foreground="White"
                                   FontSize="34"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="Analise, valide e limpe caches com execução segura, logs vivos e progresso em tempo real — sem alterar os alvos de limpeza existentes."
                                   Foreground="#DFFFFFFF"
                                   FontSize="14"
                                   Margin="0,10,28,20"
                                   MaxWidth="650"
                                   TextWrapping="Wrap"/>

                        <WrapPanel>
                            <ui:Button Command="{Binding AnalyzeCommand, ElementName=RootPage}"
                                       IsEnabled="{Binding CanAnalyze, ElementName=RootPage}"
                                       Appearance="Primary"
                                       Style="{StaticResource ActionButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Search24" Margin="0,0,8,0"/>
                                    <TextBlock Text="{x:Static p:Resources.Cleanup_ActionAnalyze}"/>
                                </StackPanel>
                            </ui:Button>

                            <ui:Button Command="{Binding CleanupSelectedCommand, ElementName=RootPage}"
                                       IsEnabled="{Binding CanCleanup, ElementName=RootPage}"
                                       Appearance="Secondary"
                                       Style="{StaticResource ActionButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Delete24" Margin="0,0,8,0"/>
                                    <TextBlock Text="{x:Static p:Resources.Cleanup_ActionClean}"/>
                                </StackPanel>
                            </ui:Button>

                            <ui:Button Command="{Binding CancelCommand, ElementName=RootPage}"
                                       Visibility="{Binding CancelVisibility, ElementName=RootPage}"
                                       Appearance="Secondary"
                                       Style="{StaticResource ActionButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Dismiss24" Margin="0,0,8,0"/>
                                    <TextBlock Text="{x:Static p:Resources.Cleanup_ActionCancel}"/>
                                </StackPanel>
                            </ui:Button>
                        </WrapPanel>
                    </StackPanel>

                    <Grid Grid.Column="1" Width="188" Height="188" Margin="20,0,0,0" VerticalAlignment="Center">
                        <Ellipse Fill="#25FFFFFF"/>
                        <Ellipse Width="138" Height="138" Fill="#35FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <ui:SymbolIcon Symbol="Broom24" FontSize="76" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.15*"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="0.85*"/>
                </Grid.ColumnDefinitions>

                <ui:Card x:Name="OptionsCard" Grid.Column="0" Padding="22" RenderTransformOrigin="0.5,0.5">
                    <ui:Card.RenderTransform>
                        <TranslateTransform Y="0"/>
                    </ui:Card.RenderTransform>
                    <StackPanel>
                        <DockPanel Margin="0,0,0,16">
                            <Border DockPanel.Dock="Left" Width="44" Height="44" CornerRadius="14" Background="#1F0078D4" Margin="0,0,12,0">
                                <ui:SymbolIcon Symbol="Checkmark24" Foreground="#0078D4" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Resources.Cleanup_SelectOptions}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="Seleção preservada: os mesmos módulos de limpeza continuam disponíveis." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DockPanel>

                        <UniformGrid Columns="2">
                            <CheckBox IsChecked="{Binding CleanWindowsUpdate, ElementName=RootPage}" Style="{StaticResource CleanupOptionStyle}">
                                <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="ArrowSync24" Foreground="#4FC3F7" Margin="0,0,8,0"/><TextBlock Text="{x:Static p:Resources.Cleanup_OptionWindowsUpdate}" TextWrapping="Wrap"/></StackPanel>
                            </CheckBox>
                            <CheckBox IsChecked="{Binding CleanTemp, ElementName=RootPage}" Style="{StaticResource CleanupOptionStyle}">
                                <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="Delete24" Foreground="#81C784" Margin="0,0,8,0"/><TextBlock Text="{x:Static p:Resources.Cleanup_OptionTemp}" TextWrapping="Wrap"/></StackPanel>
                            </CheckBox>
                            <CheckBox IsChecked="{Binding CleanSystemTemp, ElementName=RootPage}" Style="{StaticResource CleanupOptionStyle}">
                                <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="Delete24" Foreground="#4DB6AC" Margin="0,0,8,0"/><TextBlock Text="{x:Static p:Resources.Cleanup_OptionSystemTemp}" TextWrapping="Wrap"/></StackPanel>
                            </CheckBox>
                            <CheckBox IsChecked="{Binding CleanPrefetch, ElementName=RootPage}" Style="{StaticResource CleanupOptionStyle}">
                                <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="DataHistogram20" Foreground="#FFB74D" Margin="0,0,8,0"/><TextBlock Text="{x:Static p:Resources.Cleanup_OptionPrefetch}" TextWrapping="Wrap"/></StackPanel>
                            </CheckBox>
                            <CheckBox IsChecked="{Binding CleanBrowser, ElementName=RootPage}" Style="{StaticResource CleanupOptionStyle}">
                                <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="CodeText20" Foreground="#FFD54F" Margin="0,0,8,0"/><TextBlock Text="{x:Static p:Resources.Cleanup_OptionBrowser}" TextWrapping="Wrap"/></StackPanel>
                            </CheckBox>
                            <CheckBox IsChecked="{Binding CleanDns, ElementName=RootPage}" Style="{StaticResource CleanupOptionStyle}">
                                <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="Info24" Foreground="#9575CD" Margin="0,0,8,0"/><TextBlock Text="{x:Static p:Resources.Cleanup_OptionDNS}" TextWrapping="Wrap"/></StackPanel>
                            </CheckBox>
                            <CheckBox IsChecked="{Binding CleanRecycleBin, ElementName=RootPage}" Style="{StaticResource CleanupOptionStyle}">
                                <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="Delete24" Foreground="#A1887F" Margin="0,0,8,0"/><TextBlock Text="{x:Static p:Resources.Cleanup_OptionRecycleBin}" TextWrapping="Wrap"/></StackPanel>
                            </CheckBox>
                        </UniformGrid>
                    </StackPanel>
                </ui:Card>

                <ui:Card x:Name="StatusCard" Grid.Column="2" Padding="22" RenderTransformOrigin="0.5,0.5">
                    <ui:Card.RenderTransform>
                        <TranslateTransform Y="0"/>
                    </ui:Card.RenderTransform>
                    <StackPanel>
                        <TextBlock Text="Telemetria da execução" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                        <TextBlock Text="Progresso suave, contadores e seleção atual sem bloquear a interface." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,18" TextWrapping="Wrap"/>

                        <UniformGrid Columns="2">
                            <Border Style="{StaticResource MetricCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Potencial" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{Binding TotalPotentialLabel, ElementName=RootPage}" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource MetricCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Selecionadas" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{Binding SelectedScanCountLabel, ElementName=RootPage}" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <ProgressBar Height="8" Minimum="0" Maximum="100" Value="{Binding CleanupProgressPercentage, ElementName=RootPage}" Margin="0,6,0,12"/>
                        <TextBlock Text="{Binding CleanupProgressCategory, ElementName=RootPage}" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding CleanupProcessedItemsLabel, ElementName=RootPage}" Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </ui:Card>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.05*"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="0.95*"/>
                </Grid.ColumnDefinitions>

                <ui:Card x:Name="SummaryCard" Grid.Column="0" Padding="0" RenderTransformOrigin="0.5,0.5" Visibility="{Binding ShouldShowSummaryCard, ElementName=RootPage, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ui:Card.RenderTransform>
                        <TranslateTransform Y="0"/>
                    </ui:Card.RenderTransform>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Padding="18,14" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" CornerRadius="8,8,0,0">
                            <DockPanel>
                                <ui:SymbolIcon DockPanel.Dock="Left" Symbol="DataHistogram20" Margin="0,0,10,0"/>
                                <TextBlock Text="{x:Static p:Resources.Cleanup_ScanSummaryTitle}" FontSize="16" FontWeight="SemiBold"/>
                            </DockPanel>
                        </Border>

                        <Grid Grid.Row="1" Margin="18">
                            <TextBlock Text="{x:Static p:Resources.Cleanup_ScanSummaryEmpty}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Visibility="{Binding HasScanResults, ElementName=RootPage, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>

                            <ItemsControl ItemsSource="{Binding ScanResults, ElementName=RootPage}" Visibility="{Binding HasScanResults, ElementName=RootPage, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Opacity" Value="0"/>
                                        <Setter Property="RenderTransform"><Setter.Value><TranslateTransform Y="18"/></Setter.Value></Setter>
                                        <Style.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.32" EasingFunction="{StaticResource FluidEase}"/>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.32" EasingFunction="{StaticResource FluidEase}"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="14" Padding="14" Margin="0,0,0,10" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                                    <TextBlock Text="{Binding ItemsLabel}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                </StackPanel>
                                                <Border Grid.Column="2" CornerRadius="12" Padding="10,6" Background="#180078D4" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding SizeLabel}" FontWeight="SemiBold" Foreground="#0078D4"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Grid>
                </ui:Card>

                <ui:Card x:Name="LogsCard" Grid.Column="2" Padding="0" RenderTransformOrigin="0.5,0.5" MinHeight="360" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch">
                    <ui:Card.RenderTransform>
                        <TranslateTransform Y="0"/>
                    </ui:Card.RenderTransform>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Padding="18,14" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" CornerRadius="8,8,0,0">
                            <DockPanel>
                                <ui:SymbolIcon DockPanel.Dock="Left" Symbol="CodeText20" Margin="0,0,10,0"/>
                                <StackPanel>
                                    <TextBlock Text="{x:Static p:Resources.Cleanup_LogTitle}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="Eventos são renderizados em fila para manter fluidez e responsividade." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>

                        <RichTextBox Grid.Row="1" x:Name="LogOutput" IsReadOnly="True" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Background="Transparent" BorderThickness="0" Padding="18" VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top" FontFamily="Segoe UI" FontSize="12">
                            <FlowDocument PagePadding="0" TextAlignment="Left" FontFamily="Segoe UI"/>
                        </RichTextBox>

                        <TextBlock Grid.Row="1" IsHitTestVisible="False" Text="{x:Static p:Resources.Cleanup_LogEmpty}" Margin="18" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Top" Visibility="{Binding HasLogs, ElementName=RootPage, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                    </Grid>
                </ui:Card>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
