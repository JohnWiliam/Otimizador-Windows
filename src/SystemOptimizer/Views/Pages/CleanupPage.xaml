<Page x:Class="SystemOptimizer.Views.Pages.CleanupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:p="clr-namespace:SystemOptimizer.Properties"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="900"
      x:Name="RootPage"
      Title="CleanupPage"
      FontFamily="Segoe UI">

    <Page.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Margin" Value="0,0,8,8" />
        </Style>

        <Style x:Key="SummaryMetricCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="Margin" Value="0,0,8,8" />
            <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{x:Static p:Resources.Cleanup_Title}"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                   FontSize="26"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <ui:CardExpander x:Name="OptionsCard"
                         Grid.Row="1"
                         Margin="0,0,0,16"
                         IsExpanded="{Binding IsOptionsExpanded, ElementName=RootPage, Mode=TwoWay}">
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:SymbolIcon Symbol="Broom24" FontSize="24" Margin="0,0,12,0" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{x:Static p:Resources.Cleanup_CacheTitle}" FontSize="15" FontWeight="Medium" TextWrapping="Wrap" />
                        <TextBlock Text="{x:Static p:Resources.Cleanup_CacheDesc}" FontSize="13" Margin="0,4,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </ui:CardExpander.Header>

            <StackPanel Margin="0,8,0,0">
                <WrapPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <ui:Button Style="{StaticResource ActionButtonStyle}" Command="{Binding AnalyzeCommand, ElementName=RootPage}" IsEnabled="{Binding CanAnalyze, ElementName=RootPage}" Appearance="Primary">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Search24" Margin="0,0,8,0" />
                            <TextBlock Text="{x:Static p:Resources.Cleanup_ActionAnalyze}" />
                        </StackPanel>
                    </ui:Button>
                    <ui:Button Style="{StaticResource ActionButtonStyle}" Command="{Binding CancelCommand, ElementName=RootPage}" Visibility="{Binding CancelVisibility, ElementName=RootPage}" Appearance="Secondary" Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Dismiss24" Margin="0,0,8,0" />
                            <TextBlock Text="{x:Static p:Resources.Cleanup_ActionCancel}" />
                        </StackPanel>
                    </ui:Button>
                </WrapPanel>

                <TextBlock Text="{x:Static p:Resources.Cleanup_SelectOptions}" FontWeight="SemiBold" Margin="0,0,0,12" />

                <WrapPanel Orientation="Horizontal" ItemWidth="300" Margin="0,0,0,4">
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionTemp}" IsChecked="{Binding CleanTemp, ElementName=RootPage}" Margin="0,0,16,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionSystemTemp}" IsChecked="{Binding CleanSystemTemp, ElementName=RootPage}" Margin="0,0,16,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionPrefetch}" IsChecked="{Binding CleanPrefetch, ElementName=RootPage}" Margin="0,0,16,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionWindowsUpdate}" IsChecked="{Binding CleanWindowsUpdate, ElementName=RootPage}" Margin="0,0,16,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionBrowser}" IsChecked="{Binding CleanBrowser, ElementName=RootPage}" Margin="0,0,16,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionDNS}" IsChecked="{Binding CleanDns, ElementName=RootPage}" Margin="0,0,16,12" />
                    <CheckBox Content="{x:Static p:Resources.Cleanup_OptionRecycleBin}" IsChecked="{Binding CleanRecycleBin, ElementName=RootPage}" Margin="0,0,16,12" />
                </WrapPanel>
            </StackPanel>
        </ui:CardExpander>

        <ui:Card x:Name="SummaryCard"
                 Grid.Row="2"
                 Padding="0"
                 RenderTransformOrigin="0.5,0.5"
                 VerticalAlignment="Stretch"
                 Visibility="{Binding ShouldShowSummaryCard, ElementName=RootPage, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ui:Card.RenderTransform>
                <TranslateTransform Y="0" />
            </ui:Card.RenderTransform>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="16,10" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" CornerRadius="4,4,0,0">
                    <DockPanel LastChildFill="True">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left">
                            <ui:SymbolIcon Symbol="DataHistogram20" Margin="0,0,10,0" FontSize="16" />
                            <TextBlock Text="{Binding SummaryTitle, ElementName=RootPage}" FontWeight="Medium" FontSize="13" />
                        </StackPanel>
                        <TextBlock DockPanel.Dock="Right" VerticalAlignment="Center" Text="{Binding SelectedSummaryLabel, ElementName=RootPage}" />
                    </DockPanel>
                </Border>

                <StackPanel Grid.Row="1" Margin="16,12,16,16">
                    <Border Style="{StaticResource SummaryMetricCardStyle}" Visibility="{Binding IsBusyLocal, ElementName=RootPage, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="{x:Static p:Resources.Cleanup_ProgressCardTitle}" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <ProgressBar Height="8" Minimum="0" Maximum="100" Value="{Binding DataContext.CleanupProgressPercentage, ElementName=RootPage}" />
                            <TextBlock Margin="0,8,0,4" Text="{Binding BusyStatusLabel, ElementName=RootPage}" />
                            <TextBlock Text="{Binding CleanupProcessedItemsLabel, ElementName=RootPage}" />
                        </StackPanel>
                    </Border>

                    <WrapPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Border Style="{StaticResource SummaryMetricCardStyle}">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Resources.Cleanup_SummarySelectedCategories}" FontSize="12" />
                                <TextBlock Text="{Binding SelectedCategoriesCount, ElementName=RootPage}" FontWeight="SemiBold" FontSize="16" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource SummaryMetricCardStyle}">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Resources.Cleanup_SummaryPotentialTotal}" FontSize="12" />
                                <TextBlock Text="{Binding TotalScanSizeLabel, ElementName=RootPage}" FontWeight="SemiBold" FontSize="16" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource SummaryMetricCardStyle}">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Resources.Cleanup_SummaryFoundItems}" FontSize="12" />
                                <TextBlock Text="{Binding TotalScanItemsLabel, ElementName=RootPage}" FontWeight="SemiBold" FontSize="16" />
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <WrapPanel Orientation="Horizontal" Margin="0,0,0,8" Visibility="{Binding HasScanResults, ElementName=RootPage, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ui:Button Style="{StaticResource ActionButtonStyle}" Appearance="Primary" Command="{Binding CleanupSelectedCommand, ElementName=RootPage}" IsEnabled="{Binding CanCleanup, ElementName=RootPage}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Delete24" Margin="0,0,8,0" />
                                <TextBlock Text="{x:Static p:Resources.Cleanup_ActionClean}" />
                            </StackPanel>
                        </ui:Button>
                        <ui:Button Style="{StaticResource ActionButtonStyle}" Command="{Binding SelectAllCommand, ElementName=RootPage}" IsEnabled="{Binding HasScanResults, ElementName=RootPage}">
                            <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="CheckboxChecked24" Margin="0,0,8,0" /><TextBlock Text="{x:Static p:Resources.Cleanup_SummaryActionSelectAll}" /></StackPanel>
                        </ui:Button>
                        <ui:Button Style="{StaticResource ActionButtonStyle}" Command="{Binding SelectRecommendedCommand, ElementName=RootPage}" IsEnabled="{Binding HasScanResults, ElementName=RootPage}">
                            <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="Sparkle24" Margin="0,0,8,0" /><TextBlock Text="{x:Static p:Resources.Cleanup_SummaryActionSelectRecommended}" /></StackPanel>
                        </ui:Button>
                        <ui:Button Style="{StaticResource ActionButtonStyle}" Command="{Binding DeselectAllCommand, ElementName=RootPage}" IsEnabled="{Binding HasScanResults, ElementName=RootPage}">
                            <StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="CheckboxUnchecked24" Margin="0,0,8,0" /><TextBlock Text="{x:Static p:Resources.Cleanup_SummaryActionClearSelection}" /></StackPanel>
                        </ui:Button>
                    </WrapPanel>

                    <TextBlock Text="{x:Static p:Resources.Cleanup_ScanSummaryEmpty}" Margin="0,0,0,8" Visibility="{Binding HasScanResults, ElementName=RootPage, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />

                    <Grid Margin="0,0,0,8" Visibility="{Binding HasScanResults, ElementName=RootPage, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="8,0,8,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="120" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="1" Text="Categoria" FontSize="12" Opacity="0.8" />
                            <TextBlock Grid.Column="2" Text="Itens" FontSize="12" Opacity="0.8" HorizontalAlignment="Right" />
                            <TextBlock Grid.Column="3" Text="Tamanho" FontSize="12" Opacity="0.8" HorizontalAlignment="Right" />
                        </Grid>

                        <ItemsControl Grid.Row="1" ItemsSource="{Binding ScanResults, ElementName=RootPage}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,8" Padding="12,10" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="130" />
                                                <ColumnDefinition Width="120" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" Margin="0,0,10,0" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,12,0" />
                                            <TextBlock Grid.Column="2" Text="{Binding ItemsLabel}" Margin="0,0,12,0" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                            <TextBlock Grid.Column="3" Text="{Binding SizeLabel}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>

                    <Border Style="{StaticResource SummaryMetricCardStyle}" Visibility="{Binding HasLastCleanupResult, ElementName=RootPage, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="{x:Static p:Resources.Cleanup_ResultTitle}" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <UniformGrid Columns="3">
                                <StackPanel Margin="0,0,8,8"><TextBlock FontSize="12" Text="{x:Static p:Resources.Cleanup_ResultCategories}"/><TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding LastCleanupCategoriesLabel, ElementName=RootPage}"/></StackPanel>
                                <StackPanel Margin="0,0,8,8"><TextBlock FontSize="12" Text="{x:Static p:Resources.Cleanup_ResultRemovedSize}"/><TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding LastCleanupRemovedSizeLabel, ElementName=RootPage}"/></StackPanel>
                                <StackPanel Margin="0,0,8,8"><TextBlock FontSize="12" Text="{x:Static p:Resources.Cleanup_ResultRemovedItems}"/><TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding LastCleanupRemovedItemsLabel, ElementName=RootPage}"/></StackPanel>
                                <StackPanel Margin="0,0,8,8"><TextBlock FontSize="12" Text="{x:Static p:Resources.Cleanup_ResultIgnoredItems}"/><TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding LastCleanupIgnoredItemsLabel, ElementName=RootPage}"/></StackPanel>
                                <StackPanel Margin="0,0,8,8"><TextBlock FontSize="12" Text="{x:Static p:Resources.Cleanup_ResultFailedItems}"/><TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding LastCleanupFailuresLabel, ElementName=RootPage}"/></StackPanel>
                                <StackPanel Margin="0,0,8,8"><TextBlock FontSize="12" Text="{x:Static p:Resources.Cleanup_ResultDuration}"/><TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding LastCleanupDurationLabel, ElementName=RootPage}"/></StackPanel>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ui:Card>
    </Grid>
</Page>
