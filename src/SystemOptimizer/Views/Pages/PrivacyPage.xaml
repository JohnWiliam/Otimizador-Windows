<Page x:Class="SystemOptimizer.Views.Pages.PrivacyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:SystemOptimizer.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PrivacyPage">

    <Grid Margin="20,10,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Privacidade" 
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                   FontSize="24" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,10" />

        <ui:DynamicScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding PrivacyTweaks}" 
                          ItemTemplate="{StaticResource TweakItemTemplate}" />
        </ui:DynamicScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <ui:Button Content="Restaurar Seleção" Command="{Binding RevertSelectedCommand}" CommandParameter="Privacy" Appearance="Secondary" Margin="0,0,10,0" Width="150" />
            <ui:Button Content="Aplicar Seleção" Command="{Binding ApplySelectedCommand}" CommandParameter="Privacy" Appearance="Primary" Width="150" />
        </StackPanel>
    </Grid>
</Page>
