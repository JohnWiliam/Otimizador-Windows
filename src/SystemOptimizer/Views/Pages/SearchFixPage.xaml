<Page x:Class="SystemOptimizer.Views.Pages.SearchFixPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:SystemOptimizer.ViewModels"
      xmlns:p="clr-namespace:SystemOptimizer.Properties"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance vm:SearchFixViewModel}"
      Title="SearchFixPage"
      ScrollViewer.VerticalScrollBarVisibility="Auto">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="{x:Static p:Resources.SearchFix_Title}" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="{x:Static p:Resources.SearchFix_Subtitle}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,5,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Top">
            <ui:CardExpander IsExpanded="True" Margin="0,0,0,12">
                <ui:CardExpander.Icon><ui:SymbolIcon Symbol="Search24" /></ui:CardExpander.Icon>
                <ui:CardExpander.Header>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static p:Resources.SearchFix_Card1_Header}" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                        <Border Grid.Column="1" Background="Transparent" Padding="8,4" CornerRadius="4">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:SymbolIcon Symbol="Circle24" Filled="True" FontSize="12" Foreground="{Binding ColorSuggestions}" Margin="0,1,6,0"/>
                                <TextBlock Text="{Binding TextSuggestions}" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </ui:CardExpander.Header>
                <StackPanel>
                    <TextBlock Text="{x:Static p:Resources.SearchFix_Card1_Desc}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,0,0,12" FontSize="12" TextWrapping="Wrap"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <ui:Button Grid.Column="0" Content="{x:Static p:Resources.SearchFix_Apply}" Appearance="Primary" Command="{Binding ApplySuggestionsCommand}" Margin="0,0,8,0">
                            <ui:Button.Icon><ui:SymbolIcon Symbol="Checkmark20" /></ui:Button.Icon>
                        </ui:Button>
                        <ui:Button Grid.Column="1" Content="{x:Static p:Resources.SearchFix_Revert}" Appearance="Secondary" Command="{Binding RevertSuggestionsCommand}">
                            <ui:Button.Icon><ui:SymbolIcon Symbol="ArrowUndo20" /></ui:Button.Icon>
                        </ui:Button>
                    </Grid>
                </StackPanel>
            </ui:CardExpander>

            <ui:CardExpander IsExpanded="True" Margin="0,0,0,12">
                <ui:CardExpander.Icon><ui:SymbolIcon Symbol="CloudOff24" /></ui:CardExpander.Icon>
                <ui:CardExpander.Header>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static p:Resources.SearchFix_Card2_Header}" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                        <Border Grid.Column="1" Background="Transparent" Padding="8,4" CornerRadius="4">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:SymbolIcon Symbol="Circle24" Filled="True" FontSize="12" Foreground="{Binding ColorCloud}" Margin="0,1,6,0"/>
                                <TextBlock Text="{Binding TextCloud}" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </ui:CardExpander.Header>
                <StackPanel>
                    <TextBlock Text="{x:Static p:Resources.SearchFix_Card2_Desc}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,0,0,12" FontSize="12" TextWrapping="Wrap"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <ui:Button Grid.Column="0" Content="{x:Static p:Resources.SearchFix_Apply}" Appearance="Primary" Command="{Binding ApplyCloudCommand}" Margin="0,0,8,0">
                            <ui:Button.Icon><ui:SymbolIcon Symbol="Checkmark20" /></ui:Button.Icon>
                        </ui:Button>
                        <ui:Button Grid.Column="1" Content="{x:Static p:Resources.SearchFix_Revert}" Appearance="Secondary" Command="{Binding RevertCloudCommand}">
                            <ui:Button.Icon><ui:SymbolIcon Symbol="ArrowUndo20" /></ui:Button.Icon>
                        </ui:Button>
                    </Grid>
                </StackPanel>
            </ui:CardExpander>

            <ui:CardExpander IsExpanded="True" Margin="0,0,0,12">
                <ui:CardExpander.Icon><ui:SymbolIcon Symbol="DismissCircle24" /></ui:CardExpander.Icon>
                <ui:CardExpander.Header>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Static p:Resources.SearchFix_Card3_Header}" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                        <Border Grid.Column="1" Background="Transparent" Padding="8,4" CornerRadius="4">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:SymbolIcon Symbol="Circle24" Filled="True" FontSize="12" Foreground="{Binding ColorBing}" Margin="0,1,6,0"/>
                                <TextBlock Text="{Binding TextBing}" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </ui:CardExpander.Header>
                <StackPanel>
                    <TextBlock Text="{x:Static p:Resources.SearchFix_Card3_Desc}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,0,0,12" FontSize="12" TextWrapping="Wrap"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <ui:Button Grid.Column="0" Content="{x:Static p:Resources.SearchFix_Apply}" Appearance="Primary" Command="{Binding ApplyBingCommand}" Margin="0,0,8,0">
                            <ui:Button.Icon><ui:SymbolIcon Symbol="Checkmark20" /></ui:Button.Icon>
                        </ui:Button>
                        <ui:Button Grid.Column="1" Content="{x:Static p:Resources.SearchFix_Revert}" Appearance="Secondary" Command="{Binding RevertBingCommand}">
                            <ui:Button.Icon><ui:SymbolIcon Symbol="ArrowUndo20" /></ui:Button.Icon>
                        </ui:Button>
                    </Grid>
                </StackPanel>
            </ui:CardExpander>
        </StackPanel>

        <Border Grid.Row="2" Background="{DynamicResource ControlFillColorDefaultBrush}" Padding="20,15" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{x:Static p:Resources.SearchFix_Footer}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </StackPanel>
                <ui:Button Grid.Column="1" Content="{x:Static p:Resources.SearchFix_Btn_Restart}" Appearance="Danger" Command="{Binding RestartExplorerCommand}">
                    <ui:Button.Icon><ui:SymbolIcon Symbol="ArrowClockwise20" /></ui:Button.Icon>
                </ui:Button>
            </Grid>
        </Border>
    </Grid>
</Page>
