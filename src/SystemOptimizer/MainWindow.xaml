<ui:FluentWindow x:Class="SystemOptimizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:SystemOptimizer.ViewModels"
        xmlns:pages="clr-namespace:SystemOptimizer.Views.Pages" 
        xmlns:p="clr-namespace:SystemOptimizer.Properties"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.App_Title}"
        Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" 
                     Title="{x:Static p:Resources.App_Title}" 
                     Padding="10,0" />

        <ui:NavigationView Grid.Row="1"
                           x:Name="RootNavigation"
                           Padding="0"
                           PaneDisplayMode="Left"
                           IsPaneOpen="True"
                           IsBackButtonVisible="Collapsed">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="{x:Static p:Resources.Nav_Privacy}" Icon="Shield24" TargetPageType="{x:Type pages:PrivacyPage}" />
                <ui:NavigationViewItem Content="{x:Static p:Resources.Nav_Performance}" Icon="TopSpeed24" TargetPageType="{x:Type pages:PerformancePage}" />
                <ui:NavigationViewItem Content="{x:Static p:Resources.Nav_Network}" Icon="Globe24" TargetPageType="{x:Type pages:NetworkPage}" />
                <ui:NavigationViewItem Content="{x:Static p:Resources.Nav_Security}" Icon="LockClosed24" TargetPageType="{x:Type pages:SecurityPage}" />
                <ui:NavigationViewItem Content="{x:Static p:Resources.Nav_Visual}" Icon="Color24" TargetPageType="{x:Type pages:AppearancePage}" />
                <ui:NavigationViewItem Content="{x:Static p:Resources.Nav_Search}" Icon="Search24" TargetPageType="{x:Type pages:SearchPage}" />
                <ui:NavigationViewItem Content="{x:Static p:Resources.Nav_Cleanup}" Icon="Broom24" TargetPageType="{x:Type pages:CleanupPage}" />
                
                <ui:NavigationViewItemSeparator />
                
                <ui:NavigationViewItem Content="{x:Static p:Resources.Nav_Tweaks}" Icon="Wrench24" TargetPageType="{x:Type pages:TweaksPage}" />
            </ui:NavigationView.MenuItems>
            
            <ui:NavigationView.FooterMenuItems>
                 <ui:NavigationViewItem Content="{x:Static p:Resources.Settings_Title}" Icon="Settings24" TargetPageType="{x:Type pages:SettingsPage}" />
            </ui:NavigationView.FooterMenuItems>
        </ui:NavigationView>

        <ContentPresenter x:Name="RootContentDialogPresenter" 
                          Grid.Row="0" Grid.RowSpan="2" />

        <ui:SnackbarPresenter x:Name="SnackbarPresenter" 
                              Grid.Row="0" Grid.RowSpan="2" />

    </Grid>
</ui:FluentWindow>
